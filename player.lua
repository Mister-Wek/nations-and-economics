local player = {}

player.settings = {}
player.settings.map_mode = 1
player.display_organization = 1

player.settings_values = {}
player.settings_values.map_mode = {"normal", "organizations"}

return player